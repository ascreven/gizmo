import { CountryPathPipe } from './country-path.pipe';

describe('CountryPathPipe', () => {
  it('create an instance', () => {
    const pipe = new CountryPathPipe();
    expect(pipe).toBeTruthy();
  });
});
